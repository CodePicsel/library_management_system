#spring.application.name=lmsBackend
#
#spring.datasource.url=jdbc:postgresql://postgres.railway.internal:5432/railway
#spring.datasource.username=postgres
#spring.datasource.password=vISyIOmSkfNstjGtMnSNyEExIfekAFol
#
#spring.jpa.hibernate.ddl-auto=update
#spring.jpa.show-sql=true
#spring.jpa.open-in-view=false
#
#logging.level.org.springframework.security=DEBUG
#server.port=${PORT:8080}
spring.application.name=lmsBackend

# If Railway provides JDBC_DATABASE_URL, use it. Otherwise, use your local Lms_db.
spring.datasource.url=${JDBC_DATABASE_URL:jdbc:postgresql://postgres.railway.internal:5432/railway}
spring.datasource.username=${PGUSER:postgres}
spring.datasource.password=${PGPASSWORD:Harshal@2003}

# Essential for hosting
server.port=${PORT:8080}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.open-in-view=false

logging.level.org.springframework.security=DEBUG

#spring.application.name=lmsBackend
#
## The variable name must match exactly what you named it in Step 1
#spring.datasource.url=${JDBC_DATABASE_URL}
#spring.datasource.username=${PGUSER}
#spring.datasource.password=${PGPASSWORD}
#
## This is mandatory for Railway to detect your app!
#server.port=${PORT:8080}
#
#spring.jpa.hibernate.ddl-auto=update
#spring.jpa.show-sql=true